# Opens the upstream Desktop local-daemon gate: when the shared app-server
# daemon under CODEX_HOME is healthy, Desktop attaches as a websocket client
# instead of spawning a private stdio child. Unhealthy daemon -> stdio fallback.
CODEX_APP_SERVER_USE_LOCAL_DAEMON=1
